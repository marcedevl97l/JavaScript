// Notación
//; -> Delimitar el final de una linea, dejo de ser obligatorio, pero para evitar errores siempre hay que usarlo
const b = 4;

//. -> Se utiliza en los objetos para acceder a los atributos
// movil.anchura


//[] -> Se utilizan para las listas, arreglos o arrays
const ar = [1, 2, 3, 4];
console.log(ar[2])

//() -> Se utilizan para funciones
function suma (a, b) {
  // Se escribe la funcion
}

//{} -> Llaves para objetos funciones y estructuras de control
const movil = {
  anchura: 10,
  altura: 2
}

if (true) {
  // todo lo que haya entre llaves
  const constante2 = "Hola"
}